# Original request

> The verbatim ask `/create-project` is building toward. The drive-to-done loop
> (`reference/drive-to-done-loop.md`) re-reads this every cycle so the goal can't drift
> across a long build or a context reset. Replace/extend the seed below with the user's
> full original prompt.

Update REDSEC loadout site with live-verified July 2026 meta data per class and fix HTML defects, browser-verified
